const menu=document.querySelector('.menu'),nav=document.querySelector('#nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')});document.querySelectorAll('.chip').forEach(chip=>chip.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active'));chip.classList.add('active')}));const frame=document.querySelector('.frame-button'),input=document.querySelector('#question'),empty=document.querySelector('.empty-map'),generated=document.querySelector('.generated-map'),state=document.querySelector('.map-state');frame.addEventListener('click',()=>{const value=input.value.trim();if(!value){input.focus();input.setAttribute('aria-invalid','true');return}input.removeAttribute('aria-invalid');generated.querySelector('.map-question p').textContent=value;empty.hidden=true;generated.hidden=false;state.textContent='FRAME READY';frame.innerHTML='Reframe the question <span>↻</span>'});
