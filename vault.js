const cfg=window.AP_CONFIG||{},status=document.querySelector('#status'),login=document.querySelector('#login'),dash=document.querySelector('#dashboard');let client=null;function show(session){login.style.display=session?'none':'block';dash.classList.toggle('active',!!session)}if(!cfg.SUPABASE_URL||!cfg.SUPABASE_ANON_KEY){status.textContent='Vault backend is not configured yet.'}else{client=supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);client.auth.getSession().then(({data})=>show(data.session));client.auth.onAuthStateChange((_e,s)=>show(s))}document.querySelector('#loginForm').onsubmit=async e=>{e.preventDefault();if(!client)return;status.textContent='Signing in…';const {error}=await client.auth.signInWithPassword({email:email.value,password:password.value});status.textContent=error?'Sign-in failed. Check credentials or contact the Vault administrator.':''};document.querySelector('#signout').onclick=()=>client?.auth.signOut();
