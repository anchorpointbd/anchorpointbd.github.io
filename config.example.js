// Copy to config.js during deployment. The anon key is designed for browsers;
// authorization still depends on Row Level Security. NEVER put service_role here.
window.AP_CONFIG={SUPABASE_URL:"",SUPABASE_ANON_KEY:""};
